---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = '404 - Pagina no encontrada | ImprimeArte';
const description =
  'Ups. Esta pagina no existe. Vuelve al inicio o explora productos personalizados.';
---

<BaseLayout title={title} description={description}>
  <section class="im-404">
    <div class="im-404__bg" aria-hidden="true">
      <span class="im-404__blob im-404__blob--c"></span>
      <span class="im-404__blob im-404__blob--m"></span>
      <span class="im-404__blob im-404__blob--y"></span>
      <span class="im-404__blob im-404__blob--k"></span>
      <span class="im-404__rings"></span>
    </div>

    <div class="im-404__grain" aria-hidden="true"></div>

    <div class="im-404__wrap">
      <div class="im-404__hero">
        <p class="im-404__eyebrow im-404__reveal" style="--delay: 0.05s;">
          ImprimeArte / CMYK Lab
        </p>
        <h1 class="im-404__code im-404__reveal" style="--delay: 0.1s;">404</h1>
        <h2 class="im-404__title im-404__reveal" style="--delay: 0.2s;">
          Pagina fuera de registro
        </h2>
        <p class="im-404__desc im-404__reveal" style="--delay: 0.3s;">
          La tinta encontro el borde, pero no el producto. Ajusta el tiraje y
          vuelve a imprimir tu ruta.
        </p>

        <div class="im-404__actions im-404__reveal" style="--delay: 0.4s;">
          <a class="im-404__btn im-404__btn--primary" href="/">
            Volver al inicio
          </a>
          <a class="im-404__btn" href="/productos">Explorar productos</a>
          <a class="im-404__btn" href="/contacto">Hablar con el equipo</a>
        </div>

        <div class="im-404__hint im-404__reveal" style="--delay: 0.5s;">
          Sugerencia: revisa la URL o prueba en <strong>/productos</strong>.
        </div>
      </div>

      <div class="im-404__poster im-404__reveal" style="--delay: 0.25s;">
        <div class="im-404__ticket">
          <div class="im-404__ticket-top">
            <span>IMPRESION DETENIDA</span>
            <span>#404</span>
          </div>
          <div class="im-404__ticket-body">
            <div class="im-404__cmyk" aria-hidden="true">
              <span>C</span>
              <span>M</span>
              <span>Y</span>
              <span>K</span>
            </div>
            <p class="im-404__ticket-copy">
              Recalibrando colores. Ajusta el registro y continua tu compra.
            </p>
          </div>
        </div>

        <div class="im-404__stack">
          <article class="im-404__card im-404__card--c">
            <h3>Plancha C</h3>
            <p>Mapa de ruta: inicio, productos, contacto.</p>
          </article>
          <article class="im-404__card im-404__card--m">
            <h3>Plancha M</h3>
            <p>Personaliza, guarda y vuelve cuando quieras.</p>
          </article>
          <article class="im-404__card im-404__card--y">
            <h3>Plancha Y</h3>
            <p>Explora ofertas y encuentra nuevas ideas.</p>
          </article>
          <article class="im-404__card im-404__card--k">
            <h3>Plancha K</h3>
            <p>Si todo falla, nuestro soporte responde rapido.</p>
          </article>
        </div>
      </div>
    </div>

    <div class="im-404__ticker" aria-hidden="true">
      <div class="im-404__ticker-track">
        ERROR 404 · IMPRESION DETENIDA · REGISTRO CMYK · VOLVER AL INICIO ·
        ERROR 404 · IMPRESION DETENIDA · REGISTRO CMYK · VOLVER AL INICIO ·
      </div>
    </div>
  </section>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Archivo+Black&family=Archivo:wght@300;400;600&display=swap');

    .im-404 {
      --cmyk-c: #00b3e6;
      --cmyk-m: #e1007a;
      --cmyk-y: #ffd400;
      --cmyk-k: #111111;
      --paper: #fdf8f4;
      --ink: #1b1b1f;
      --shadow: rgba(17, 17, 17, 0.12);
      position: relative;
      overflow: hidden;
      min-height: calc(100vh - var(--header-height));
      padding: 3.5rem 1.5rem 6rem;
      background:
        radial-gradient(circle at 10% 20%, rgba(0, 179, 230, 0.2), transparent 45%),
        radial-gradient(circle at 90% 10%, rgba(225, 0, 122, 0.18), transparent 35%),
        radial-gradient(circle at 85% 90%, rgba(255, 212, 0, 0.2), transparent 40%),
        linear-gradient(135deg, #f7fbff 0%, #fff6f0 45%, #fffef3 70%, #f6f6f6 100%);
      font-family: 'Archivo', 'Trebuchet MS', sans-serif;
      color: var(--ink);
    }

    .im-404__bg {
      position: absolute;
      inset: -10% -10% auto -10%;
      height: 70%;
      pointer-events: none;
      z-index: 0;
    }

    .im-404__blob {
      position: absolute;
      width: 320px;
      height: 320px;
      border-radius: 40% 60% 55% 45%;
      filter: blur(0);
      mix-blend-mode: multiply;
      opacity: 0.3;
      animation: float 18s ease-in-out infinite;
    }

    .im-404__blob--c {
      background: var(--cmyk-c);
      top: 10%;
      left: 8%;
    }

    .im-404__blob--m {
      background: var(--cmyk-m);
      top: -5%;
      right: 18%;
      animation-delay: -3s;
    }

    .im-404__blob--y {
      background: var(--cmyk-y);
      bottom: -25%;
      right: 8%;
      animation-delay: -6s;
    }

    .im-404__blob--k {
      background: #000000;
      bottom: -15%;
      left: 25%;
      opacity: 0.18;
      animation-delay: -9s;
    }

    .im-404__rings {
      position: absolute;
      inset: 20% 10% auto auto;
      width: 220px;
      height: 220px;
      border-radius: 50%;
      border: 2px dashed rgba(0, 0, 0, 0.1);
      animation: spin 24s linear infinite;
    }

    .im-404__grain {
      position: absolute;
      inset: 0;
      background-image: radial-gradient(rgba(0, 0, 0, 0.12) 1px, transparent 0);
      background-size: 22px 22px;
      opacity: 0.08;
      mix-blend-mode: multiply;
      pointer-events: none;
      z-index: 0;
    }

    .im-404__wrap {
      position: relative;
      z-index: 1;
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2.5rem;
      align-items: center;
    }

    .im-404__hero {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    .im-404__eyebrow {
      text-transform: uppercase;
      letter-spacing: 0.3rem;
      font-size: 0.75rem;
      color: rgba(17, 17, 17, 0.6);
    }

    .im-404__code {
      font-family: 'Archivo Black', 'Impact', sans-serif;
      font-size: clamp(4rem, 12vw, 8rem);
      margin: 0;
      line-height: 1;
      letter-spacing: -0.05em;
      color: var(--cmyk-k);
    }

    .im-404__title {
      font-size: clamp(1.6rem, 3vw, 2.4rem);
      margin: 0;
      font-weight: 600;
    }

    .im-404__desc {
      font-size: 1.05rem;
      line-height: 1.6;
      color: rgba(17, 17, 17, 0.75);
      max-width: 480px;
    }

    .im-404__actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .im-404__btn {
      padding: 0.85rem 1.4rem;
      border-radius: 999px;
      border: 2px solid rgba(17, 17, 17, 0.15);
      font-weight: 600;
      text-decoration: none;
      color: var(--ink);
      background: rgba(255, 255, 255, 0.7);
      transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    }

    .im-404__btn:hover {
      transform: translateY(-2px);
      border-color: rgba(17, 17, 17, 0.35);
      box-shadow: 0 10px 20px rgba(17, 17, 17, 0.15);
    }

    .im-404__btn--primary {
      background: linear-gradient(120deg, var(--cmyk-c), var(--cmyk-m));
      border: none;
      color: #fff;
      box-shadow: 0 12px 30px rgba(0, 179, 230, 0.3);
    }

    .im-404__hint {
      padding: 0.75rem 1rem;
      border-radius: 0.75rem;
      background: rgba(255, 255, 255, 0.7);
      border: 1px solid rgba(17, 17, 17, 0.08);
      font-size: 0.95rem;
    }

    .im-404__poster {
      display: grid;
      gap: 1.5rem;
    }

    .im-404__ticket {
      background: var(--paper);
      border-radius: 1.5rem;
      box-shadow: 0 20px 40px var(--shadow);
      border: 1px solid rgba(17, 17, 17, 0.08);
      overflow: hidden;
    }

    .im-404__ticket-top {
      display: flex;
      justify-content: space-between;
      padding: 0.85rem 1.5rem;
      font-size: 0.75rem;
      letter-spacing: 0.2rem;
      text-transform: uppercase;
      background: linear-gradient(90deg, rgba(0, 179, 230, 0.2), rgba(225, 0, 122, 0.2));
    }

    .im-404__ticket-body {
      padding: 1.5rem;
      display: grid;
      gap: 1rem;
    }

    .im-404__cmyk {
      display: inline-flex;
      gap: 0.75rem;
      font-family: 'Archivo Black', 'Impact', sans-serif;
      font-size: 1.5rem;
      letter-spacing: 0.05em;
    }

    .im-404__cmyk span:nth-child(1) {
      color: var(--cmyk-c);
    }
    .im-404__cmyk span:nth-child(2) {
      color: var(--cmyk-m);
    }
    .im-404__cmyk span:nth-child(3) {
      color: var(--cmyk-y);
    }
    .im-404__cmyk span:nth-child(4) {
      color: var(--cmyk-k);
    }

    .im-404__ticket-copy {
      margin: 0;
      color: rgba(17, 17, 17, 0.75);
    }

    .im-404__stack {
      display: grid;
      gap: 1rem;
    }

    .im-404__card {
      padding: 1.1rem 1.2rem;
      border-radius: 1rem;
      background: #fff;
      box-shadow: 0 10px 20px rgba(17, 17, 17, 0.08);
      border: 1px solid rgba(17, 17, 17, 0.08);
      transform: translateY(0);
      transition: transform 0.2s ease;
    }

    .im-404__card h3 {
      margin: 0 0 0.35rem;
      font-size: 1rem;
    }

    .im-404__card p {
      margin: 0;
      color: rgba(17, 17, 17, 0.65);
      font-size: 0.95rem;
    }

    .im-404__card--c {
      border-left: 6px solid var(--cmyk-c);
    }
    .im-404__card--m {
      border-left: 6px solid var(--cmyk-m);
    }
    .im-404__card--y {
      border-left: 6px solid var(--cmyk-y);
    }
    .im-404__card--k {
      border-left: 6px solid var(--cmyk-k);
    }

    .im-404__card:hover {
      transform: translateY(-3px);
    }

    .im-404__ticker {
      position: absolute;
      inset: auto 0 0 0;
      background: #111;
      color: #fff;
      overflow: hidden;
      padding: 0.6rem 0;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.2rem;
      z-index: 1;
    }

    .im-404__ticker-track {
      display: inline-block;
      white-space: nowrap;
      animation: ticker 22s linear infinite;
      padding-left: 100%;
    }

    .im-404__reveal {
      opacity: 0;
      transform: translateY(16px);
      animation: fadeUp 0.9s ease forwards;
      animation-delay: var(--delay, 0s);
    }

    @keyframes float {
      0%,
      100% {
        transform: translate3d(0, 0, 0) scale(1);
      }
      50% {
        transform: translate3d(20px, -18px, 0) scale(1.04);
      }
    }

    @keyframes spin {
      from {
        transform: rotate(0deg);
      }
      to {
        transform: rotate(360deg);
      }
    }

    @keyframes ticker {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(-100%);
      }
    }

    @keyframes fadeUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 720px) {
      .im-404 {
        padding: 2.5rem 1.2rem 5rem;
      }

      .im-404__actions {
        flex-direction: column;
        align-items: stretch;
      }

      .im-404__btn {
        text-align: center;
      }

      .im-404__ticker {
        font-size: 0.7rem;
        letter-spacing: 0.12rem;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .im-404__blob,
      .im-404__rings,
      .im-404__ticker-track,
      .im-404__reveal {
        animation: none;
      }
      .im-404__reveal {
        opacity: 1;
        transform: none;
      }
    }
  </style>
</BaseLayout>
