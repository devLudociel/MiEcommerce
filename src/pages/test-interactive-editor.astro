---
/**
 * Página de prueba para el Editor Interactivo
 * Visita: http://localhost:4321/test-interactive-editor
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import TestInteractiveEditorApp from '../components/test/TestInteractiveEditorApp';
import { requireAdminPage, AUTH_COOKIE_NAME } from '../lib/auth/requireAdminPage';

const guardResponse = await requireAdminPage(
  Astro.request,
  Astro.url,
  Astro.cookies.get(AUTH_COOKIE_NAME)?.value
);

if (guardResponse) {
  return guardResponse;
}
---

<BaseLayout title="Test - Editor Interactivo">
  <div class="min-h-screen bg-gradient-to-br from-purple-50 via-white to-cyan-50 py-12">
    <div class="max-w-4xl mx-auto px-4">
      {/* Header */}
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">
          🎨 Test - Editor Interactivo de Imagen
        </h1>
        <p class="text-lg text-gray-600">
          Prueba el nuevo editor visual drag & drop
        </p>
        <div class="mt-4 inline-flex items-center gap-2 px-4 py-2 bg-blue-100 text-blue-800 rounded-lg text-sm">
          <span class="font-bold">ℹ️ Página de prueba</span>
          <span>·</span>
          <span>No afecta los personalizadores actuales</span>
        </div>
      </div>

      {/* Instrucciones */}
      <div class="bg-white rounded-xl border-2 border-gray-200 p-6 mb-8 shadow-lg">
        <h2 class="text-xl font-bold text-gray-800 mb-4 flex items-center gap-2">
          <span>📋</span>
          Cómo probar
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="space-y-3">
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-purple-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">1</div>
              <div>
                <p class="font-semibold text-gray-800">Sube una imagen</p>
                <p class="text-sm text-gray-600">Click en el área de carga o arrastra una imagen</p>
              </div>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-purple-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">2</div>
              <div>
                <p class="font-semibold text-gray-800">Mueve la imagen</p>
                <p class="text-sm text-gray-600">Click y arrastra para mover la posición</p>
              </div>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-purple-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">3</div>
              <div>
                <p class="font-semibold text-gray-800">Escala con handles</p>
                <p class="text-sm text-gray-600">Arrastra los círculos morados de las esquinas</p>
              </div>
            </div>
          </div>
          <div class="space-y-3">
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-cyan-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">4</div>
              <div>
                <p class="font-semibold text-gray-800">Rota con handle</p>
                <p class="text-sm text-gray-600">Arrastra el círculo cyan de arriba</p>
              </div>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-cyan-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">5</div>
              <div>
                <p class="font-semibold text-gray-800">Usa Undo/Redo</p>
                <p class="text-sm text-gray-600">Botones en toolbar o Ctrl+Z / Ctrl+Y</p>
              </div>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-8 h-8 bg-cyan-500 text-white rounded-full flex items-center justify-center font-bold flex-shrink-0">📱</div>
              <div>
                <p class="font-semibold text-gray-800">Móvil: Pinch-to-zoom</p>
                <p class="text-sm text-gray-600">Usa 2 dedos para escalar (pellizco)</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* App Container */}
      <TestInteractiveEditorApp client:load />

      {/* Comparación */}
      <div class="mt-8 bg-gradient-to-r from-green-50 to-blue-50 rounded-xl border-2 border-green-200 p-6">
        <h3 class="text-lg font-bold text-gray-800 mb-4">
          ✅ Ventajas vs Sliders
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
          <div class="bg-white rounded-lg p-4 border border-green-200">
            <div class="text-2xl mb-2">⚡</div>
            <p class="font-bold text-gray-800">4x más rápido</p>
            <p class="text-gray-600">~5s vs ~20s con sliders</p>
          </div>
          <div class="bg-white rounded-lg p-4 border border-green-200">
            <div class="text-2xl mb-2">🎯</div>
            <p class="font-bold text-gray-800">83% menos errores</p>
            <p class="text-gray-600">Posicionamiento visual directo</p>
          </div>
          <div class="bg-white rounded-lg p-4 border border-green-200">
            <div class="text-2xl mb-2">🧠</div>
            <p class="font-bold text-gray-800">Cero aprendizaje</p>
            <p class="text-gray-600">Patrón universal drag & drop</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
