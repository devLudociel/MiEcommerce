---
import Layout from '../../../layouts/BaseLayout.astro';
import CategoryPageOptimized from '../../../components/pages/CategoryPageOptimized';

const { categorySlug, subcategorySlug } = Astro.params;
---

<Layout title={`${subcategorySlug} - ${categorySlug}`}>
  <CategoryPageOptimized client:load categorySlug={categorySlug} subcategorySlug={subcategorySlug} />
</Layout>
